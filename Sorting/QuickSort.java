package Sorting;
//tbd
public class QuickSort {
    
    public void qSort(int[] arr){
        if(arr.length <= 0) return;

        sorter(arr, 0, arr.length);

    }

    private void sorter(int[] arr, int low, int high){
        while(low < high){

        }
    }

    private int partition(arr, low, high){

    }

}
